╔════════════════════════════════════════════════════════════════╗
║                                                                ║
║        🔧 DATABASE MIGRATION - COPY & PASTE THIS SQL          ║
║                                                                ║
╚════════════════════════════════════════════════════════════════╝

WHERE TO RUN:
1. Go to: https://rdrlxmpwkkeryfcszltc.supabase.co
2. Click: SQL Editor (left sidebar)
3. Click: New Query button
4. Paste the SQL below
5. Click: Run (or press Ctrl+Enter / Cmd+Enter)

────────────────────────────────────────────────────────────────

-- Add missing columns to repairs table
ALTER TABLE repairs 
ADD COLUMN IF NOT EXISTS status VARCHAR(50) DEFAULT 'in_progress',
ADD COLUMN IF NOT EXISTS final_issue_type VARCHAR(50),
ADD COLUMN IF NOT EXISTS updated_at TIMESTAMP DEFAULT NOW();

-- Create indexes for performance
CREATE INDEX IF NOT EXISTS idx_repairs_status ON repairs(status);
CREATE INDEX IF NOT EXISTS idx_repairs_final_issue_type ON repairs(final_issue_type);

-- Update existing records
UPDATE repairs SET status = 'in_progress' WHERE status IS NULL;
UPDATE repairs SET updated_at = created_at WHERE updated_at IS NULL;

-- Verify changes (optional - check results)
SELECT column_name, data_type, column_default 
FROM information_schema.columns 
WHERE table_name = 'repairs' 
ORDER BY ordinal_position;

────────────────────────────────────────────────────────────────

✅ EXPECTED OUTPUT:
   Success. No rows returned

✅ THEN YOU'LL SEE (from verification query):
   - id (uuid)
   - request_id (uuid)  
   - garage_id (uuid)
   - ai_summary (text)
   - mechanic_notes (text)
   - created_at (timestamp)
   - status (varchar) ← NEW!
   - final_issue_type (varchar) ← NEW!
   - updated_at (timestamp) ← NEW!

⏱️  TIME: 30 seconds

────────────────────────────────────────────────────────────────

AFTER RUNNING, TELL ME:
"Migration complete"

AND I WILL:
✅ Verify all columns exist
✅ Test all API endpoints  
✅ Test all filters
✅ Test complete user flow
✅ Generate success report

╔════════════════════════════════════════════════════════════════╗
║  🎯 That's it! Just copy the SQL above and run it in Supabase ║
╚════════════════════════════════════════════════════════════════╝
